export type MessageSchema = {
  ui: {
    test: string
    vocabCount: string
    loading: string
    error: string
    arrow: string
  }
  nav: {
    learn: string
    review: string
    settings: string
  }
}